/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{html,js,jsx,ts,tsx}", // All files in the `src` folder
  "./*.html", // All HTML files in the root directory
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }